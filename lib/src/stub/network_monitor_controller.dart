/// Minimal stub controller — monitoring features are always off.
class NetworkMonitorController {
  /// Always `false` in the stub.
  bool get isMonitoringEnabled => false;

  /// Always `false` in the stub.
  bool get isDevModeEnabled => false;
}
