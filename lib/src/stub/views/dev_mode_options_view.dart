import 'package:flutter/material.dart';

/// Stub settings route — [push] is a no-op.
class DevModeOptionsView {
  const DevModeOptionsView._();

  /// No-op when using the stub entry point.
  static void push(BuildContext context) {}
}
